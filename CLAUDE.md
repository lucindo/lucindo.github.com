# lucindo.github.io

Before changing anything on this site, read `SITE.md` — it defines the site's
purpose, hard rules (single self-contained HTML file, no external requests),
design tokens, theme-switching contract, and the snippet for adding a new app.
